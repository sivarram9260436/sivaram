source 'http://rubygems.org' 
gem 'rails', '3.1.3' 
# Postgres db adapter 
gem 'pg'

gem 'roo'
# Gems used only for assets and not required 
# in production environments by default. 
# group :assets do 
#   gem 'sass-rails',   '~> 3.1.4' 
#   gem 'coffee-rails', '~> 3.1.1' 
#   gem 'uglifier', '>= 1.0.3' 
# end 
gem 'jquery-rails' 
# To use ActiveModel has_secure_password 
# gem 'bcrypt-ruby', '~> 3.0.0' 
# Use unicorn as the web server 
 gem 'unicorn' 
#gem 'Sub-Engine',:require => "Sub-Engine", :path => '../Sub-Engine' 
# Deploy with Capistrano 
 gem 'capistrano' 
# To use debugger 
 gem 'ruby-debug19','>= 0.5.11' 
 gem 'linecache19', :git => 'git://github.com/mark-moseley/linecache' 
 gem 'ruby-debug-base19x', '~> 0.11.30.pre4' 
 gem 'debugger'
#gem 'jquery_datepicker' 
group :test do 
  # Pretty printed test output 
  gem 'turn', :require => false 
end 
#gem added by kingston for testing purpose 
gem 'rails-dev-boost', :git =>'git://github.com/thedarkone/rails-dev-boost.git', :require =>'rails_development_boost' 
# Kreatio Specific Gems 
 gem 'fastercsv' 
 #gem 'libxml-ruby'  # Fast XML parser 
 gem 'nokogiri' # Parser 
 gem 'sanitize' # To Sanitsie params 
 gem 'rmagick', :require => 'RMagick' # For image processing uses image magick internally 
 gem 'chronic' # Used for formatting Time 
 gem 'crack' # For Parsing XML and JSON 
 gem 'acts_as_ferret', '>= 0.5' # Used for ferret search. Used for generating summary in Kreatio in search results. 
 gem 'htmlentities' # Used for encoding and decoding HTML entities 
 gem 'mime-types' # Used to find Mime Type of a file. Might have been used in file upload to identify the files type. 
 gem 'builder' # XML Builder 
 gem 'net-ssh' # For SSH 
 gem 'net-sftp' # For FTP 
 gem 'xhtmldiff' # Takes a diff betweek two XHTML, planned to use for history function in articles 
 gem 'diff-lcs' # Takes a diff betweek two XHTML, planned to use for history function in articles 
 gem "xml-simple" # Simple and easy XML parser. 
 gem "mixable_engines" # Planned to use multiple engine to override the Core Kreatio Kernel 
 gem 'will_paginate', '>=3.0.pre' # Used for pagination 
 gem 'memcache-client' # Used as API to store data in memcache. 
 gem 'simple_captcha' 
# gem "mail", "2.1.3" 
# Used for building forms easily 
 gem 'formtastic' 
#Used for date picker 
gem 'jquery_datepicker' 
# Used for managing tree assosiations in active record.  
 gem 'acts_as_tree' 
 gem 'sneaky-save' 
 # simple captcha & recaptcha 
 gem "recaptcha", :require => "recaptcha/rails" 
 gem 'galetahub-simple_captcha', :require => 'simple_captcha', :git => 'git://github.com/galetahub/simple-captcha.git'
 #gem 'json_builder' 
 #gem 'rails_autolink' 

gem 'will_paginate', '>=3.0.pre' #for pagination

gem 'rack-mini-profiler', require: false

# For memory profiling (requires Ruby MRI 2.1+)
#gem 'memory_profiler'

# For call-stack profiling flamegraphs (requires Ruby MRI 2.0.0+)
gem 'flamegraph'
#gem 'stackprof'     # For Ruby MRI 2.1+
gem 'fast_stack'    # For Ruby MRI 2.0

